- project ||= @project
- suite ||= @suite
- page ||= @page
- request ||= @request
- assertion ||= @assertion
br
ul.breadcrumbs
  li = link_to "Home", root_url
  li = link_to project.title, project_url(project)
  li = link_to suite.title, project_suite_url(project, suite)
  li = link_to page.title, project_suite_page_url(project, suite, page)
  li = link_to request.title, project_suite_page_request_url(project, suite, page, request)
  li = link_to request.title, project_suite_page_request_assertion_url(project, suite, page, request, assertion)
  li Edit Assertion

h2 Edit Assertion
== render 'form'
